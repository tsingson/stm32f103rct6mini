# 启用基础 GPIO
CONFIG_GPIO=y

# 启用现代串口控制台
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_PRINTK=y

# 显式使用 C17 标准（Zephyr 4.4+ 默认开启，无需再配置旧的 CONFIG_STD_C11=y）
# ==================== Zephyr 4.4.1 核心电源管理配置 ====================
CONFIG_PM=y
CONFIG_PM_DEVICE=y
